# =============================================================================
# DevTeam Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# --- AI Provider API Keys ---
# Anthropic (Claude) - Used by DEV, CQ, QA
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxx

# x.ai (Grok) - Used by PO
XAI_API_KEY=xai-xxxxxxxxxxxxx

# OpenAI (GPT) - Used by OPS
OPENAI_API_KEY=sk-xxxxxxxxxxxxx

# --- Human Communication (PO -> Human stakeholders) ---
# Type: meeting-board (default), discord, or slack
HUMAN_COMMS_TYPE=meeting-board
# Webhook URL for Discord or Slack (leave empty for meeting-board type)
HUMAN_COMMS_WEBHOOK_URL=

# --- Project Board (local TaskBoard) ---
# Runs locally in Docker — no external URL needed
# External port for human access to the board UI
PROJECT_BOARD_PORT=8088
# Session secret for the Project Board web UI
PB_SESSION_SECRET=devteam-session-secret-change-me
# Default password for seeded accounts (human + agents)
PB_DEFAULT_PASSWORD=devteam2025

# --- Project Board API Tokens (one per agent) ---
# These are auto-generated on first startup and printed to the project-board logs.
# Run: docker compose logs project-board | grep "API Tokens" -A 10
# Then paste the tokens here and restart the agents.
PB_TOKEN_PO=paste-token-from-project-board-logs
PB_TOKEN_DEV=paste-token-from-project-board-logs
PB_TOKEN_CQ=paste-token-from-project-board-logs
PB_TOKEN_QA=paste-token-from-project-board-logs
PB_TOKEN_OPS=paste-token-from-project-board-logs

# --- Meeting Board ---
# Internal URL used by bots (Docker service name)
MEETING_BOARD_URL=http://meeting-board:8080
# External port for human dashboard
MEETING_BOARD_PORT=8080

# --- Meeting Board Auth Tokens (one per bot) ---
# Format: role:token pairs, comma-separated
# Generate unique tokens for each bot, e.g.: openssl rand -hex 32
MB_TOKEN_PO=change-me-po-token
MB_TOKEN_DEV=change-me-dev-token
MB_TOKEN_CQ=change-me-cq-token
MB_TOKEN_QA=change-me-qa-token
MB_TOKEN_OPS=change-me-ops-token

# Composite token string for the meeting board service
AUTH_TOKENS=po:${MB_TOKEN_PO},dev:${MB_TOKEN_DEV},cq:${MB_TOKEN_CQ},qa:${MB_TOKEN_QA},ops:${MB_TOKEN_OPS}

# --- MongoDB ---
MONGO_URI=mongodb://mongo:27017
MONGO_DB=meetingboard

# --- Project Code Path ---
# Path to the project code that DEV works on (mounted into DEV and CQ containers)
PROJECT_CODE_PATH=./project

# --- QA Evidence ---
# Path where QA saves screenshots and test evidence (mounted from QA container)
QA_EVIDENCE_PATH=./qa-evidence

# --- Docker Socket ---
# OPS needs access to Docker socket for deployments
DOCKER_SOCKET=/var/run/docker.sock
